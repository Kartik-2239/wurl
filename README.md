So we can make it so that some functions resolve what kind of content it is and then display it in that way,
like for markdown using markdown renderer
for image we can use kitty protocol
for json we can use rich json renderer
and we can just use text/{language} for syntax highlighting.